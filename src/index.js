import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './components/App/App';

import './index.css';

//remember to change component name later to App...
ReactDOM.createRoot(document.getElementById('app')).render(<App />);
